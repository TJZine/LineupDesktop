# Import Ledger

Every copied or adapted upstream Lineup slice must be recorded here before or in
the same change that imports it.

## Required Fields

| Field | Meaning |
| --- | --- |
| Source path | Upstream repo path or source document |
| Source commit/date | Upstream commit, branch, or exact source date |
| Imported files/symbols | Files or symbols copied/adapted into this repo |
| Desktop owner | Owning module or document in this repo |
| Adaptation summary | What changed and why |
| Retained tests | Tests copied or preserved from upstream |
| New tests needed | Desktop-specific proof still required |
| Platform assumptions | WebOS/browser assumptions removed, isolated, or pending |
| Divergence rationale | Why copy/adapt is better than exact sharing for now |
| Provenance notes | License or binary/source provenance notes |
| Follow-up trigger | When to revisit, extract, or replan |

## Entries

| Source path | Source commit/date | Imported files/symbols | Desktop owner | Adaptation summary | Retained tests | New tests needed | Platform assumptions | Divergence rationale | Provenance notes | Follow-up trigger |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `docs/architecture/desktop-repo-genesis-adr.md` from upstream Lineup workspace | 2026-05-07, upstream base `08c4e0fe` plus reviewed local ADR | `docs/architecture/desktop-repo-genesis-adr.md` | Architecture docs | Adapted links and wording for this separate repo; removed upstream cleanup-control references as active authority | Current repo `npm run verify:docs` passed before scaffold | Keep this repo `verify:docs` passing | Upstream webOS/current cleanup state is evidence only | Separate repo needs local authority without importing upstream control-plane baggage | Apache-2.0 project source | Revisit if repo shape, playback hypothesis, or secret ownership changes |
| Upstream Lineup workflow conventions | 2026-05-07, upstream base `08c4e0fe` | `AGENTS.md`, `docs/AGENTIC_DEV_WORKFLOW.md`, `docs/agentic/*` | Workflow/control plane | Ported steady-state feature workflow and review discipline; omitted historical cleanup program mechanics | Current repo docs verifier informed structure | This repo docs verifier and harness tests | Upstream webOS verification routing adapted to desktop scaffold | Desktop needs workflow from day one, but not exact upstream cleanup history | Apache-2.0 project source | Revisit after first workflow-harness review |
| Upstream Lineup Codex role convention | 2026-05-07, upstream base `08c4e0fe` | `.codex/config.toml`, `.codex/agents/*.toml` | Codex role config | Adapted conservative role set for Desktop; omitted dedicated maintenance-worker role until this repo has its own need | None | `npm run verify:docs`; role configs reviewed as control-plane surface | Upstream role text had web app history; Desktop role text is product-neutral and anti-overengineering | Codex roles are repo-local config, not shared package code | Apache-2.0 project source | Revisit after first Tier 3 Desktop plan and implementation |
| Upstream Lineup Codanna convention | 2026-05-07, upstream base `08c4e0fe` | `.codannaignore`, `docs/agentic/codanna-playbook.md` | Discovery workflow | Adapted to path-agnostic Desktop guidance; did not copy generated `.codanna/` index or absolute settings | None | `npm run verify:docs` | Old index paths were tied to the upstream checkout | Desktop needs discovery rules, not old generated state | Apache-2.0 project source | Revisit when a fresh Desktop Codanna index is initialized |
| Upstream Lineup review and repo-hosting conventions | 2026-05-07, upstream base `08c4e0fe` | `.coderabbit.yaml`, `.github/**`, `SECURITY.md`, `LICENSE`, `.editorconfig` | Repo hosting and review config | Rewritten for Desktop Electron boundaries, Windows CI, pre-release security policy, and current verification scripts | None | `npm run verify`; future GitHub CI runs `npm run verify` on Linux and Windows | Old webOS jobs and path instructions were not copied | Desktop needs own review/CI surface before product code | Apache-2.0 project source | Revisit after Electron dependencies and runtime smoke tests exist |
| Upstream Lineup architecture-lint pattern | 2026-05-07, upstream base `08c4e0fe` | `eslint.config.js`, `tools/architecture-rules/*`, `tools/__tests__/build-eslint-architecture-rules.test.mjs` | Architecture verification | Rebuilt as first-pass Electron boundary linting instead of copying web app module rules | Harness tests for rule builder | `npm run verify:architecture`; `npm run test:harness-docs` | Old webOS module roots are not Desktop roots | Desktop needs boundary linting shaped around renderer/preload/main/native-helper | Apache-2.0 project source | Revisit when source directories are created and real Electron imports land |
| GPT Pro desktop port handoff report | 2026-05-07, user-provided local planning artifact | `docs/roadmap/desktop-port-roadmap.md` | Roadmap/control plane | Adapted the report's phase table, reuse guidance, risk gates, and first-task recommendations into this repo's greenfield Desktop sequence; kept the active secure shell foundation first because this repo has no runtime yet | None | `npm run verify:docs`; future roadmap slices require their own tracked plans and verification | Report assumed the original Lineup repo shape; roadmap adapts it to the separate Desktop repo and does not import product source | Local planning artifact distilled into tracked Desktop authority so future agents do not depend on chat memory | Planning report from user-provided local file; no product source copied | Revisit after the secure Electron shell foundation is implemented and after each product import slice |
| `src/modules/plex/library/*` and metadata-only `src/modules/plex/shared/types.ts` from upstream Lineup | `bb3da904aa87be6c6a00c25e6fa340f16f788709`, observed 2026-05-10 | `src/main/plex/library/types.ts`, `src/main/plex/library/plexLibraryError.ts`, `src/main/plex/library/constants.ts`, `src/main/plex/library/parsing/*`, `src/main/plex/library/libraryDomain.ts`, `src/main/plex/library/index.ts`, `src/contracts/plex.ts`, parser/domain symbols for library sections, media items/files/streams, seasons, collections, playlists, tag directories, search hubs, request intent, tag directory media-type policy, pagination guards, and renderer-safe summaries | `src/main/plex/library/**` and `src/contracts/plex.ts` | Adapted upstream library parser/domain behavior into Desktop main-owned modules; removed upstream app-error and logging dependencies; kept metadata parsing only; added renderer-safe summary helpers that omit raw payload, media file, image key, URI-like, header-like, and secret-like fields; no live fetch, cache runtime, image URL construction, stream resolver runtime, preload, renderer, or main composition imported | Upstream parser/error/request-intent/tag-directory tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/main/plexLibrary.test.ts` | Keep `npm run typecheck`, `npm run test:contracts`, `npm run verify:redaction`, and `npm run verify` passing; implementation review for RD-10 Unit 1 | Browser/WebOS storage and runtime fetch assumptions removed; Mac/local fake-data proof is sufficient because this unit is pure parser/domain code with no live Plex or Electron runtime composition | Exact sharing would pull browser/runtime and broader upstream dependencies into Desktop; focused adaptation preserves parser behavior while enforcing main-owned redaction and renderer-safe contracts | Apache-2.0 project source; upstream workspace status for scoped Plex paths was clean at the pinned commit before import | Revisit when RD-10 later wires injected Plex transport, library cache runtime, image handling, or renderer/preload summaries, or if upstream parser behavior changes materially |
| `src/modules/plex/auth/*` from upstream Lineup | `bb3da904aa87be6c6a00c25e6fa340f16f788709`, observed 2026-05-10 | `src/main/plex/auth/types.ts`, `src/main/plex/auth/plexAuthError.ts`, `src/main/plex/auth/plexAuthPayloadParsers.ts`, `src/main/plex/auth/plexHomeUsersPayloadParser.ts`, `src/main/plex/auth/plexSwitchPayloadParser.ts`, `src/main/plex/auth/plexAuthIdentity.ts`, `src/main/plex/auth/desktopPlexCredentialStore.ts`, `src/main/plex/auth/desktopPlexAuthService.ts`, `src/main/plex/auth/index.ts`, `src/contracts/plex.ts`, parser/error/config/header/service symbols for PIN responses, user token profiles, Plex Home users, profile switch responses, Desktop identity metadata, privileged auth headers, injected auth transport actions, and renderer-safe auth/profile/credential summaries | `src/main/plex/auth/**` and `src/contracts/plex.ts` | Adapted upstream auth parser, identity, PIN polling, token validation, Plex Home user, and profile-switch behavior into Desktop main-owned modules behind an injected fakeable transport; removed upstream app-error, EventEmitter, retry fetch, localStorage client-id, browser credential storage, and live Plex service dependencies; added sanitized auth error taxonomy and a Desktop credential store adapter over RD-09 encrypted persistence that discards decrypted secret values before returning seam results | Upstream auth parser/config/error/PIN/profile-switch tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/main/plexAuth.test.ts` | Keep `npm run typecheck`, `npm run test:contracts`, `npm run verify:redaction`, and `npm run verify` passing; implementation review for RD-10 Unit 2 | Browser/WebOS localStorage, device metadata, global fetch, and live Plex transport assumptions removed; Mac/local fake storage and fake transport proof is sufficient because this unit does not wire live Plex, real Electron safeStorage/app paths, preload, renderer, or OS-specific runtime composition | Exact sharing would reintroduce browser storage, global transport, and renderer-adjacent auth state; focused adaptation preserves auth behavior while enforcing Desktop main-owned credential, transport-injection, cancellation, and redaction boundaries | Apache-2.0 project source; upstream workspace status for scoped Plex auth paths was clean at the pinned commit before import | Revisit when RD-10 wires production Electron safeStorage/app paths, live Plex transport composition, renderer/preload auth summaries, or if upstream auth parser/config/service behavior changes materially |
| `src/modules/plex/discovery/*` and `src/core/server-selection/*` selected-server behavior from upstream Lineup | `bb3da904aa87be6c6a00c25e6fa340f16f788709`, observed 2026-05-10 | `src/main/plex/discovery/types.ts`, `src/main/plex/discovery/constants.ts`, `src/main/plex/discovery/plexDiscoveryError.ts`, `src/main/plex/discovery/discoveryDomain.ts`, `src/main/plex/discovery/desktopPlexSelectedServerStore.ts`, `src/main/plex/discovery/desktopPlexServerDiscovery.ts`, `src/main/plex/discovery/index.ts`, `src/contracts/plex.ts`, parser/probe/selection symbols for resource normalization, connection priority, server health classification, selected-server restore, and renderer-safe server summaries | `src/main/plex/discovery/**` and `src/contracts/plex.ts` | Adapted upstream discovery parser, mixed-content connection probing priority, selected-server restore, stale discovery-context invalidation, and health classification into Desktop main-owned modules with injected resource/probe transport and RD-09 selected-server summary persistence; removed upstream localStorage, EventEmitter, global fetch, raw connection-change events, persisted server URI, logging, and browser storage-key behavior | Upstream discovery/probe/selected-server tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/main/plexDiscovery.test.ts` | Keep `npm run typecheck`, `npm run test:contracts`, `npm run verify:redaction`, and `npm run verify` passing; implementation review for RD-10 Unit 3 | Browser/WebOS localStorage, mixed-content UI warnings, live plex.tv fetch, and connection URI persistence assumptions removed; Mac/local fake transport and fake RD-09 storage proof is sufficient because this unit does not wire live Plex, real Electron safeStorage/app paths, preload, renderer, or OS-specific runtime composition | Exact sharing would reintroduce browser storage and URI-bearing renderer/event seams; focused adaptation preserves discovery behavior while enforcing Desktop restore-by-server-id, main-owned in-memory connection custody, and renderer-safe contracts | Apache-2.0 project source; upstream workspace status for scoped Plex discovery and server-selection paths was clean at the pinned commit before import | Revisit when RD-10 or a later roadmap item wires live Plex discovery transport, renderer/preload server selection, runtime connection observers, image/media URL setup, or if upstream discovery policy changes materially |
| `src/modules/plex/discovery/PlexDiscoveryFetchVariants.ts`, `src/modules/plex/discovery/PlexResourceDiscoveryRequestPolicy.ts`, `src/modules/plex/discovery/PlexDiscoveryRequestExecutor.ts`, `src/modules/plex/discovery/PlexDiscoveryResponsePolicy.ts`, and related discovery tests from upstream Lineup | `9a4dfcaa5d12c75ecd416cd6e81f32c21cf13be8`, observed 2026-05-15 | `src/main/plex/discovery/livePlexDiscoveryRequestPolicy.ts`, `src/main/plex/discovery/livePlexDiscoveryXmlParser.ts`, `src/main/plex/discovery/desktopPlexServerDiscovery.ts`, `src/main/plex/livePlexTransport.ts`, `src/main/plex/livePlexTransportError.ts`, and `src/__tests__/main/plexDiscovery.test.ts` variant, XML, parse-error, retry, and redaction cases | `src/main/plex/**` | Adapted upstream live discovery request policy into Desktop main-owned transport: base plex.tv resources request, trusted token-query fallback variants including clients.plex.tv, JSON-array/XML Device/Connection normalization into existing `PlexApiResource`, bounded 429/5xx retry behavior, and sanitized transport errors; preserved typed live transport discovery failures through the Desktop discovery seam; kept tokens, headers, raw URLs, raw payloads, selected connections, and retry policy inside main; split the policy/error/XML helpers out of the transport file to satisfy Desktop file-shape guardrails | Upstream discovery fetch-variant, request-policy, and response-policy tests were reviewed and represented by focused Desktop `node:test` cases in `src/__tests__/main/plexDiscovery.test.ts` | Keep `npm run test:contracts -- --test-name-pattern "plex discovery|plex runtime"`, `npm run verify:redaction`, `git diff --check`, and implementation review passing before RD-22 Unit 2A closeout | Browser/WebOS global discovery fetch policy was adapted to Electron main with injected `fetch` and internal wait injection for tests; no renderer, preload, contract, persistence, package, native helper, playback, selected-server scoping, auth Home/switch, or library behavior changed | Exact sharing would import upstream app error/logging/redaction dependencies and browser-oriented global fetch ownership; focused adaptation preserves current Desktop contracts and main-owned token custody | Apache-2.0 project source; upstream scoped discovery files were read from `/Users/tristan/Software/Lineup` at the pinned commit; unrelated upstream dirty docs/images were not used as evidence | Revisit when RD-22 later rewrites selected-server persistence/profile scoping, auth Home endpoint/switch behavior, renderer onboarding, or if upstream discovery request policy changes materially |
| `src/modules/plex/auth/plexHomeEndpointClient.ts`, `src/modules/plex/auth/plexHomeProfileClient.ts`, and related auth tests from upstream Lineup | `9a4dfcaa5d12c75ecd416cd6e81f32c21cf13be8`, observed 2026-05-15 | `src/main/plex/auth/plexHomeEndpointPolicy.ts`, `src/main/plex/auth/desktopPlexAuthService.ts`, `src/main/plex/livePlexTransport.ts`, `src/__tests__/main/plexAuth.test.ts`, and `src/__tests__/main/plexRuntimeIpc.test.ts` Plex Home endpoint/version, empty-users fallback, protected switch, PIN query, and error-classification cases | `src/main/plex/auth/**` and `src/main/plex/livePlexTransport.ts` | Adapted upstream Plex Home endpoint probing into Desktop main-owned auth service: v2-first then v1 Home users and switch endpoints, empty v2 Home users fallback before returning empty, unsupported 404/405 handling, retryable failure preservation, protected switch PIN/auth distinction, and live transport switch PIN query placement; kept account and switched profile tokens in main memory and did not add renderer/preload/contract surface | Upstream Home endpoint and Home profile client tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/main/plexAuth.test.ts` and live transport URL proof in `src/__tests__/main/plexRuntimeIpc.test.ts` | Keep `npm run typecheck`, `npm run test:contracts -- --test-name-pattern "plex auth|plex runtime"`, `npm run verify:redaction`, `npm run verify:docs`, and `git diff --check` passing before RD-22 Unit 2B closeout | Browser/WebOS fetch and app-error assumptions were adapted to Desktop injected auth transport and existing `PlexAuthError` taxonomy; no renderer, preload, contract, persistence, selected-server, playback, native helper, package, or library behavior changed | Exact sharing would import upstream transport/app-error dependencies and browser-oriented fetch helpers; focused adaptation preserves behavior while keeping endpoint fallback and token custody inside the main auth owner | Apache-2.0 project source; upstream scoped auth files were read from `/Users/tristan/Software/Lineup` at the pinned commit; unrelated upstream dirty docs/images were not used as evidence | Revisit when RD-22 later rewrites active profile persistence/restore, renderer onboarding, selected-server profile scoping, or if upstream Plex Home endpoint behavior changes materially |
| `src/core/server-selection/SelectedServerPersistenceAdapter.ts`, `src/core/server-selection/ServerSelectionCoordinator.ts`, `src/core/server-selection/SelectedServerRuntimeController.ts`, `src/core/server-selection/__tests__/*.test.ts`, `src/modules/plex/discovery/ServerSelectionStore.ts`, and selected-server restore tests from upstream Lineup | `9a4dfcaa5d12c75ecd416cd6e81f32c21cf13be8`, observed 2026-05-15 | `src/main/persistence/desktopPersistenceStore.ts`, `src/main/plex/discovery/desktopPlexSelectedServerStore.ts`, `src/main/plex/discovery/desktopPlexServerDiscovery.ts`, `src/main/plex/desktopPlexRuntime.ts`, `src/__tests__/main/persistenceBoundary.test.ts`, `src/__tests__/main/plexDiscovery.test.ts`, and `src/__tests__/main/plexRuntimeIpc.test.ts` selected-server profile scoping behavior | `src/main/persistence/**`, `src/main/plex/discovery/**`, and `src/main/plex/desktopPlexRuntime.ts` | Adapted upstream active-user selected-server persistence into Desktop main-owned storage/runtime: selected-server summaries are stored in a scoped map keyed by active profile/account id, manual selection and restore require the active profile id from auth runtime, Plex Home profile switches clear in-memory selection, restore reads only the scoped summary and reselects by fresh discovery/probing, and failed/missing restores do not overwrite another profile's scoped selection. Desktop deliberately does not persist upstream `serverUri`; raw connection URI, selected connection, token, auth headers, and server URI stay out of persistence and renderer-facing output. The legacy singleton `selectedServer` remains readable/writable only as a compatibility fallback for unscoped store callers, not the runtime path. | Upstream selected-server persistence/coordinator/runtime and discovery restore tests were reviewed and represented by focused Desktop `node:test` cases for profile A/B scoped save/restore, missing scoped selection after profile switch, raw URI exclusion, and cross-profile no-overwrite on restore miss/failure | Keep `npm run typecheck`, `npm run test:contracts -- --test-name-pattern "plex discovery|plex runtime|persistence"`, `npm run verify:redaction`, `npm run verify:docs`, `npm run verify:maintainability`, and `git diff --check` passing before RD-22 Unit 2C closeout | Browser/WebOS localStorage and credentials-file `serverUri` assumptions removed; Mac/local injected persistence and fake discovery/auth proof is sufficient because this unit changes main-owned storage/runtime only and does not add renderer/preload APIs | Exact sharing would persist raw server URI and import browser/runtime coordinator assumptions; focused adaptation preserves active-profile parity while honoring Desktop's no-raw-connection-persistence boundary and current main-only connection custody | Apache-2.0 project source; upstream scoped server-selection/discovery files were read from `/Users/tristan/Software/Lineup`; unrelated upstream dirty docs/images were not used as evidence | Revisit when renderer onboarding/server selection is wired, when migration policy for legacy global selected-server state is planned, or if upstream selected-server scoping behavior changes materially |
| `src/modules/plex/discovery/PlexServerDiscovery.ts` in-flight discovery lifecycle from upstream Lineup | `c335473cb6b97773f659631aaea3d09a1f4ef68b`, observed 2026-05-16 | `src/main/plex/discovery/desktopPlexServerDiscovery.ts` refresh coalescing and `src/__tests__/main/plexDiscovery.test.ts` concurrent/stale refresh cases | `src/main/plex/discovery/**` | Adapted upstream `_discoveryPromise` behavior into Desktop's main-owned discovery owner so concurrent refresh callers reuse the in-flight discovery request instead of multiplying Plex cloud requests. Reset still invalidates the active discovery context and permits a fresh request without allowing stale results to overwrite the current profile/server state. | Upstream `PlexServerDiscovery` discovery lifecycle behavior was represented by focused Desktop `node:test` cases for concurrent refresh coalescing and reset-during-refresh freshness. | Keep `npm run test:contracts -- --test-name-pattern "desktop plex discovery"`, `npm run verify:redaction`, `npm run verify:docs`, and `git diff --check` passing before retrying RD-22 live proof. | Upstream browser EventEmitter, localStorage, and full server-cache controller are not copied; Desktop keeps token-bearing discovery inside Electron main and leaves renderer/preload contracts unchanged. | Focused adaptation reduces live Plex rate-limit pressure without importing upstream browser ownership or broad UI/runtime controller shape. | Apache-2.0 project source; upstream scoped discovery file was read from `C:\Software\Lineup` on branch `code-health`; no raw live Plex evidence was recorded. | Revisit when RD-22 renderer onboarding is rebuilt around upstream screen flow or if live proof still hits rate limiting after a quiet retry window. |
| `src/modules/plex/library/PlexLibrary.ts`, `src/modules/plex/library/types.ts`, `src/modules/plex/library/constants.ts`, and `src/modules/plex/library/__tests__/PlexLibrary.test.ts` browse/search/metadata behavior from upstream Lineup | `9a4dfcaa5d12c75ecd416cd6e81f32c21cf13be8`, observed 2026-05-15 | `src/main/plex/desktopPlexRuntime.ts`, `src/main/plex/livePlexTransport.ts`, `src/main/plex/library/plexLibraryError.ts`, `src/contracts/plex.ts`, `src/contracts/shell.ts`, `src/preload/index.cts`, `src/__tests__/main/plexRuntimeIpc.test.ts`, `src/__tests__/integration/preloadContractVocabulary.test.ts`, and `src/__tests__/contracts/contracts.test.ts` library browse/search/metadata parity cases | `src/main/plex/**`, `src/contracts/**`, and `src/preload/index.cts` | Adapted upstream library browse/search/metadata semantics into Desktop main-owned runtime: non-positive browse limits return empty without token/transport, browse transparently follows bounded full pages and trims requested limits, sort/filter/includeCollections are forwarded through safe validated query vocabulary, search forwards safe media types and filters hubs by mapped Desktop media type, empty or 404-style metadata returns semantic `null`, and malformed successful payloads still fail through sanitized parse errors. Renderer remains unprivileged and receives only renderer-safe media summaries. | Upstream `PlexLibrary.test.ts` cases for pagination, non-positive limits, filter forwarding, search type filtering/malformed hubs, and empty/not-found metadata were reviewed and represented by focused Desktop `node:test` coverage in runtime, live transport, contract, and preload tests | Keep `npm run typecheck`, `npm run test:contracts -- --test-name-pattern "plex library|plex runtime|preload Plex|contract"`, `npm run verify:redaction`, `npm run verify:docs`, `npm run verify:maintainability`, and `git diff --check` passing before RD-22 Unit 2D closeout | Browser/WebOS cache, EventEmitter, image URL, logging, and global fetch assumptions remain removed; Desktop uses injected main transport and narrow preload validation only | Exact sharing would import upstream browser/runtime dependencies and expose broader library owner assumptions; focused adaptation preserves parity while honoring Desktop IPC, redaction, and main-owned token custody boundaries | Apache-2.0 project source; upstream scoped library files were read from `/Users/tristan/Software/Lineup`; unrelated upstream dirty docs/images were not used as evidence | Revisit when renderer onboarding consumes filter/type controls, when library cache/image handling is planned, or if upstream library browse/search/metadata behavior changes materially |
| `src/modules/ui/profile-select/**`, `src/modules/ui/server-select/**`, `src/modules/ui/channel-setup/**`, and `src/styles/shell.onboarding.*.css` from upstream Lineup | `c335473cb6b97773f659631aaea3d09a1f4ef68b`, observed 2026-05-16 | `src/renderer/staticDom.ts`, `src/renderer/plexRuntimeRows.ts`, `src/renderer/plexRuntimeDom.ts`, `src/renderer/focusDom.ts`, `src/renderer/styles/plex-onboarding.css`, `src/renderer/styles/plex-onboarding-cards.css`, `src/renderer/styles.css`, `src/renderer/styles/workflow-screens.css`, and focused renderer tests in `src/__tests__/renderer/**` | Renderer onboarding composition and CSS | Adapted upstream onboarding screen hierarchy, profile cards/PIN affordance, server rows/status actions, channel setup library/media row treatment, scroll-safe lists, focus-first product flow, and isolated onboarding route presentation into Desktop renderer-only owners. Desktop keeps Plex credential, token, transport, connection, persistence, retry, raw payload, and channel-authoring custody outside renderer and does not implement RD-23 channel creation. | Upstream profile/server/channel setup UI tests were reviewed and represented by focused Desktop `node:test` coverage for mounted Plex onboarding controls, dynamic profile/server/library/media rows, setup focus order, route-shell isolation, fake/debug surface retirement, text-entry bypass, nested back clearing, and renderer-safe summaries. | Keep focused renderer tests, `npm run verify:maintainability`, `npm run verify:redaction`, `npm run verify`, implementation review, and sanitized Windows visual/manual proof passing before RD-22 UI parity closeout | Browser/webOS screen shell, router, storage, profile-session persistence, saved-server autoconnect, channel build workflow, dropdown runtime, and direct DOM lifecycle assumptions were rewritten into static Desktop renderer composition and existing unprivileged Plex IPC controller seams | Exact sharing would import upstream browser navigation/state machines and RD-23 channel-build assumptions; focused adaptation preserves product feel while honoring Desktop process boundaries and current RD-22 scope | Apache-2.0 project source; upstream scoped UI files were read from `C:\Software\Lineup` on branch `code-health`; no raw Plex names, URLs, tokens, payloads, paths, or screenshots were recorded | Revisit when RD-23 owns channel creation, when RD-24 owns guide/player behavior, or if reviewer/user visual proof still describes the RD-22 onboarding path as scaffold UI |
| `src/modules/scheduler/scheduler/*` and `src/modules/scheduler/shared/*` from upstream Lineup | `bb3da904aa87be6c6a00c25e6fa340f16f788709`, observed 2026-05-11 | `src/domain/scheduler/shared/blockPlayback.ts`, `src/domain/scheduler/shared/playbackOrdering.ts`, `src/domain/scheduler/shared/prng.ts`, `src/domain/scheduler/constants.ts`, `src/domain/scheduler/types.ts`, `src/domain/scheduler/interfaces.ts`, `src/domain/scheduler/shuffleGenerator.ts`, `src/domain/scheduler/scheduleCalculator.ts`, `src/domain/scheduler/channelScheduler.ts`, `src/domain/scheduler/index.ts`, scheduler index/calculation/channel scheduler/shuffle/block playback symbols | `src/domain/scheduler/**` | Adapted upstream scheduler and shared playback ordering into a pure Desktop domain owner; replaced upstream channel-manager type imports with local domain-safe content item types; replaced player stream descriptor import with a nullable opaque placeholder; replaced upstream EventEmitter, `Date.now`, `globalThis` timers, and console drift warning with injected clock/timer ports and a small local event owner | Upstream scheduler and block playback tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/domain/schedulerDomain.test.ts` | Keep `npm run typecheck`, `npm run verify:architecture`, `npm run test:contracts -- --test-name-pattern "scheduler domain"`, and `npm run verify:redaction` passing; implementation review for RD-11 Unit 1 | Browser/WebOS timers and player stream descriptor assumptions removed; Mac/local injected-clock proof is sufficient because this unit is pure TypeScript with no Electron, Node, browser globals, runtime composition, Plex transport, or playback URL setup | Exact sharing would import upstream channel/player dependencies and runtime globals into `src/domain/**`; focused adaptation preserves deterministic scheduling behavior while satisfying Unit 0 domain-boundary linting | Apache-2.0 project source; upstream source evidence pinned by commit and scoped file list in the active RD-11 plan | Revisit when RD-11 channel/content domain supplies resolved items, when runtime composition injects a production clock/timer, or if upstream scheduler behavior changes materially |
| `src/modules/scheduler/channel-manager/*` channel/content domain files from upstream Lineup | `bb3da904aa87be6c6a00c25e6fa340f16f788709`, observed 2026-05-11 | `src/domain/channel/types.ts`, `src/domain/channel/interfaces.ts`, `src/domain/channel/channelError.ts`, `src/domain/channel/constants.ts`, `src/domain/channel/channelContentSourceValidator.ts`, `src/domain/channel/channelValueValidators.ts`, `src/domain/channel/channelDomainClone.ts`, `src/domain/channel/channelImportNormalizer.ts`, `src/domain/channel/channelImportExportService.ts`, `src/domain/channel/channelAuthoringService.ts`, `src/domain/channel/contentSelectionPolicy.ts`, `src/domain/channel/contentItemMapper.ts`, `src/domain/channel/sourceResolutionCache.ts`, `src/domain/channel/channelResolutionCache.ts`, `src/domain/channel/channelRetryScheduler.ts`, `src/domain/channel/contentResolver.ts`, `src/domain/channel/channelManager.ts`, `src/domain/channel/channelSafety.ts`, `src/domain/channel/index.ts`, channel authoring/import/export/content resolution/cache/retry/navigation symbols | `src/domain/channel/**` | Adapted upstream channel manager and content resolver behavior into a pure Desktop domain owner; replaced upstream app-error, EventEmitter, `Date.now`, global timers, console defaults, DOM abort construction, browser storage keys, main Plex types, and persistence coordinator with domain-owned error codes, local event owner, injected clock/timer/logger/id/library/persistence ports, and a domain-safe Plex-library-like media item shape | Upstream channel authoring, content-source validator, import normalizer, manager content-resolution/error/import-order/stale-fallback/transactional, cache, retry, resolver, and selection-policy tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/domain/channelDomain.test.ts` | Keep `npm run typecheck`, `npm run verify:architecture`, `npm run test:contracts -- --test-name-pattern "channel domain"`, and `npm run verify:redaction` passing; implementation review for RD-11 Unit 2 | Browser/WebOS `localStorage`, global timers, DOM abort object construction, live Plex transport, raw Plex payload, media URL/header, and renderer persistence assumptions removed; Mac/local injected-port proof is sufficient because this unit is pure TypeScript with no runtime composition | Exact sharing would import upstream main/Plex/runtime dependencies and browser persistence mechanics into `src/domain/**`; focused adaptation preserves channel behavior while satisfying Desktop domain-boundary and redaction policy | Apache-2.0 project source; upstream source evidence pinned by commit and scoped file list in the active RD-11 plan | Revisit when RD-11 Unit 3 adds typed channel persistence owners, when runtime composition injects production library/clock/timer/persistence ports, or if upstream channel/content behavior changes materially |
| `src/modules/scheduler/channel-manager/StoredChannelDataCodec.ts`, `ChannelPersistenceSaveQueue.ts`, `ChannelPersistenceCoordinator.ts`, `ChannelPersistenceStore.ts`, and `ChannelRepository.ts` from upstream Lineup | `bb3da904aa87be6c6a00c25e6fa340f16f788709`, observed 2026-05-11 | `src/domain/channel/storedChannelDataCodec.ts`, `src/domain/channel/channelPersistenceStore.ts`, `src/domain/channel/channelRepository.ts`, `src/domain/channel/channelPersistenceSaveQueue.ts`, `src/domain/channel/channelPersistenceCoordinator.ts`, `src/main/persistence/desktopChannelPersistenceStore.ts`, channel persistence codec/store/repository/queue/coordinator symbols and separate Desktop channel persistence file adapter | `src/domain/channel/**` and `src/main/persistence/desktopChannelPersistenceStore.ts` | Adapted upstream browser `localStorage` persistence into pure domain ports plus a main-owned file adapter with injected file path; replaced storage keys, `Date.now`, global timers, DOM quota checks, and app-error dependencies with injected storage, clock, timer, logger, and domain error seams; kept repository-owned normalized load, malformed recovery, current-channel normalization, seed repair, channel number/order repair, invalid source dropping, debounced queue, flush/dispose, and transactional full-lineup snapshot semantics | Upstream stored-data codec, persistence store, repository, save queue, coordinator, and ChannelManager persistence malformed recovery/current-channel normalization tests were reviewed and represented by focused Desktop `node:test` coverage in `src/__tests__/domain/channelPersistence.test.ts` and `src/__tests__/main/channelPersistenceAdapter.test.ts` | Keep `npm run typecheck`, `npm run verify:architecture`, `npm run test:contracts -- --test-name-pattern "channel persistence"`, and `npm run verify:redaction` passing; implementation review for RD-11 Unit 3 | Browser/WebOS `localStorage`, storage-key diagnostics, DOM quota detection, global timers, Electron app-path resolution, credential storage, selected-server state, tokens, raw Plex payloads, auth headers, URLs, and runtime/user filesystem paths are removed from the domain seam and tests; Mac/local temp-file proof is sufficient because runtime composition is not wired | Exact sharing would mix browser storage and runtime globals into `src/domain/**`; focused adaptation preserves persistence behavior while keeping channel state in a separate Desktop-owned versioned file outside the RD-09 credential/selected-server store | Apache-2.0 project source; upstream source evidence pinned by commit and scoped file list in the active RD-11 plan | Revisit when runtime composition chooses the production channel persistence path, when preload/renderer channel APIs are planned, or if upstream persistence recovery behavior changes materially |
